# Auto-RCA Report: f044594c05994df0292f366fa1f6bd794fa9e4734917f0caf447e66d9443ff88

## Incident Summary

| Field          | Value                                                                 |
|----------------|-----------------------------------------------------------------------|
| Exception      | `RuntimeException`                                                    |
| Message        | `[Auto-RCA test] Simulated 500 error triggered from the admin panel (ref rd6b99cbb6e).` |
| File           | `src/Controller/Admin/AutoRcaController.php:36`                      |
| Route          | `POST /fr/admin/auto-rca/simulate-exception`                         |
| Method         | `admin_auto_rca_simulate`                                             |
| Triggered at   | `2026-06-09T19:15:08+00:00`                                          |
| Project        | `m4kqny2hy3gry` (environment: `main`)                                |

## Root Cause Analysis

The exception was **deliberately thrown** by the `simulateException()` method in `src/Controller/Admin/AutoRcaController.php:27-40`.

This is a test endpoint that exists specifically to trigger the Auto-RCA pipeline for end-to-end verification. It:

1. Validates the CSRF token (`auto_rca_simulate` intent)
2. Generates a unique random reference via `bin2hex(random_bytes(5))` prefixed with `r` to distinguish each click (the AutoRcaSubscriber deduplicates by exception signature, so identical messages would be suppressed after the first trigger)
3. Throws a `RuntimeException` which propagates through Symfony's `ExceptionEvent`, where `AutoRcaSubscriber` (registered at priority `-100`) intercepts it and spawns an Upsun task container

There is **no code bug**. The exception message explicitly declares itself as a simulated test (`[Auto-RCA test]`).

## Verdict

**Not a production bug.** This is a deliberate test trigger from the admin test panel.

Confidence: **High** — the exception message, method name, and surrounding context all confirm this is intentional.

## Recommendation

No code change is required for this specific incident. The endpoint is working as designed:
- CSRF protection: present
- Guard for production-only spawning: handled by `AutoRcaSubscriber::isProduction` check
- Deduplication prevention: the random hex token ensures distinct signatures per click
