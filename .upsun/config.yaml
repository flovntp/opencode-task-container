routes:
    "https://{all}/": { type: upstream, upstream: "app:http" }
    "http://{all}/": { type: redirect, to: "https://{all}/" }

services: {}

applications:
    app:
        source:
            root: "/"

        type: php:8.5

        runtime:
            extensions:
                - apcu
                - blackfire
                - ctype
                - iconv
                - mbstring
                - pdo_sqlite
                - sodium
                - xsl

        variables:
            env:
                IS_PRODUCTION: "true"
            php:
                opcache.preload: config/preload.php

        build:
            flavor: none

        web:
            locations:
                "/":
                    root: "public"
                    expires: 1h
                    passthru: "/index.php"

        mounts:
            "/var/cache": { source: instance, source_path: var/cache }
            "/var/share": { source: storage, source_path: var/share }
            "/data": { source: storage, source_path: "data" }
        hooks:
            build: |
                set -x -e

                curl -fs https://get.symfony.com/cloud/configurator | bash
                
                NODE_VERSION=22 symfony-build

            deploy: |
                set -x -e

                symfony-deploy
        authorizations:
            - type: task
              resource: myagent
              action: operate
            - type: env
              action: view
        
        crons:
            security-check:
                # Check that no security issues have been found for PHP packages deployed in production
                spec: '50 23 * * *'
                commands:
                    start: if [ "$PLATFORM_ENVIRONMENT_TYPE" = "production" ]; then croncape COMPOSER_ROOT_VERSION=1.0.0 COMPOSER_AUDIT_ABANDONED=ignore composer audit --no-cache; fi
            clean-expired-sessions:
                spec: '17,47 * * * *'
                commands:
                    start: croncape php-session-clean

tasks:
  myagent:
    source:
      root: /
    type: nodejs:24
    container_profile: "HIGH_MEMORY"
    dependencies:
      nodejs:
        opencode-ai: "*" # OpenCode
        upsun: "*"       # Upsun CLI
    run:
      command: node agent/setup.js && node agent/agent.js
      timeout: 3600
    # TODO test without these permissions too, to confirm the agent can still run and collect evidence even if the CLI is completely unavailable (e.g. due to a broken install or a failed `npm install` during build).
    authorizations: # allow the task to access the environment API (read-only)
      - type: env
        action: view
      - type: task
        resource: myagent
        action: operate
    variables:
      env:
        OPENCODE_THEME: "opencode"
        HOME: "/app"
    hooks:
      build: |
        set -ex

        echo "Install Upsun Skills..."
        npx skills add https://github.com/upsun/ai --skill upsun -y
        echo "Upsun Skills installed at $PLATFORM_APP_DIR/node_modules/.bin/upsun-mcp"

        # Token optimisation: install the `snip` binary used by the opencode-snip
        # plugin. snip has no npm package, so it cannot go through dependencies;
        # install-github-asset.sh fetches it from GitHub releases into /app/.global/bin.
        #
        # Authenticate the GitHub API calls with a short-lived App installation
        # token. Unauthenticated calls share a 60 req/h bucket keyed on Upsun's
        # NAT egress IP (contended by every project on the cluster) and routinely
        # 403; an App token uses the installation's own 5000 req/h quota instead.
        echo "Minting GitHub App token for snip install..."
        set +x  # don't leak the token into the xtrace build log
        export GITHUB_API_TOKEN="$(node agent/mint-github-token.js || true)"
        set -x

        echo "Install snip (opencode-snip backend)..."
        curl -fsS https://raw.githubusercontent.com/upsun/snippets/main/src/install-github-asset.sh | bash /dev/stdin "edouard-claude/snip"
        snip --version

    relationships:
      app: "app:http"
